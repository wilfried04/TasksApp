<nav class="navbar navbar-default">
              <h2>THE TASKS</h2>
            <div class="search">
                <%= form_with(url: tasks_path, method: :get, local: true) do |f| %>
                <%= f.label(:name, 'Name:') %>
                <%= f.text_field :Name %>
                <%= f.label:statut %>
                <%= f.select :statut, [ ["Started", 0 ], ["Not_started", 1 ], ["Completed", 2 ] ]%>
                <%= f.submit'search',:class=>"btn btn-dark" %>
                <% end %>
            </div>
                <span class="nav-top">
                    <span><%=link_to " Home", tasks_path, :class=>"fas fa-home"%></span>
                    <span><%=link_to " Add new task", new_task_path,:class=>"fas fa-plus-square"%></span>
                </span>
            </div>
</nav>      